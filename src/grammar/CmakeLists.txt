# Библиотека с грамматикой и first/follow
add_library(grammar
    first_follow.cpp
    items.cpp
    symbol_utils.cpp
    generated/grammar_rules.cpp
)

target_include_directories(grammar PUBLIC
    ${CMAKE_CURRENT_SOURCE_DIR}
    ${CMAKE_CURRENT_SOURCE_DIR}/generated
)

# Опционально: для отладки
target_compile_definitions(grammar PRIVATE $<$<CONFIG:Debug>:DEBUG>)
